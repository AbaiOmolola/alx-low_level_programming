#include <stdio.h>
#include "main"

/**
 * main - prints largest of the three integers
 * Return: 0
 */

int main(void)
{
	int a, b, c;
	int largest;

	a = 927;
	b = -98;
	c = 0;

	largest = largest number(a, b, c);
	printf("%d is the largest number\n", largest);

	return (0);
}
