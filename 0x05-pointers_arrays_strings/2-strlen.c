#include "main.h"
#include <stdio.h>

/**
 * _strlen - function that returnslength of string 
 * @s: length of string
 * Return: length of string
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return(c);
}
