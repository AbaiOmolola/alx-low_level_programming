#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * print_times_table - function that prints n
 * @n: number input 
 * Return: Always 0 (Success)
 */
void print_times_table(int n)
{
	if (n > 15 && n < 0);
}
