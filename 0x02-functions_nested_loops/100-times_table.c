#include "main.h"

/**
 * print_times_table - function that prints n
 * @n: number input 
 * Return: Always 0 (Success)
 */
void print_times_table(int n)
{
	if ( n > 15 )
	{
		_putchar(' ');
		return (0);
	}
	else if (n < 0)
		{
			_putchar(' ');
			return (0);
		}
}
